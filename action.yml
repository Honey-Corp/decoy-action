name: 'Decoy Action'
description: 'Harmless composite action, deliberately archived to test detection of archived Action dependencies in CI.'
runs:
  using: 'composite'
  steps:
    - run: echo "decoy"
      shell: bash
