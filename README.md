# decoy-action

Intentionally harmless composite action. This repo exists only to be referenced from
`honeypot-public`'s workflow and then archived, so Fencer's `github_wf_archived_actions`
check has something real to flag. Do not use this action anywhere else.
